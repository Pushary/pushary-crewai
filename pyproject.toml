[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pushary-crewai"
version = "0.3.0"
description = "Phone approvals for CrewAI agents. Your agent asks, your user taps Approve or Deny."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Pushary", email = "business@pushary.com" }]
keywords = [
    "crewai",
    "human-in-the-loop",
    "ai-agents",
    "approvals",
    "basetool",
    "approval",
    "tool-approval",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "pushary>=2.0.0,<3",
    "crewai>=1.0,<2",
]

[project.urls]
Homepage = "https://pushary.com/human-in-the-loop-crewai"
Repository = "https://github.com/Pushary/pushary-crewai"
Documentation = "https://pushary.com/docs/agents/adapters"

[tool.hatch.build.targets.wheel]
packages = ["src/pushary_crewai"]

[tool.hatch.build.targets.sdist]
include = [
    "src/pushary_crewai",
    "README.md",
    "LICENSE",
    "tests",
]

[tool.pytest.ini_options]
pythonpath = ["."]
